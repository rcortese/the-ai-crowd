services:
  the-ai-crowd:
    environment:
      DOCKER_ENABLE: "true"
    group_add:
      - "${DOCKER_GID:?Set DOCKER_GID to the GID of /var/run/docker.sock on the host}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
